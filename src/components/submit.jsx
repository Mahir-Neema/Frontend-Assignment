import { Button, FormControl, FormHelperText, HStack, Radio, RadioGroup } from "@chakra-ui/react";

const SubmitButton = () => {
  return (
    <>
          <Button>Submit</Button>
    </>
  );
};

export default SubmitButton;
